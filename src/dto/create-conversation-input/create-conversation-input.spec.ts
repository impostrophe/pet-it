import { CreateConversationInput } from './create-conversation-input';

describe('CreateConversationInput', () => {
  it('should be defined', () => {
    expect(new CreateConversationInput()).toBeDefined();
  });
});
